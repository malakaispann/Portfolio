export const enum Size {
    Small = "small",
    Medium = "medium",
    Large = "large",
}

export const enum LinkType {
    Internal,
    External
}

export const enum MenuType {
    Mobile,
    Full
}